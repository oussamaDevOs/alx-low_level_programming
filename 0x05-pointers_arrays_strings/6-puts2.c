#include "main.h"
#include<string.h>

/**
 * puts2 - prints a string
 * @str: string
 */

void puts2(char *str)
{

	int i = 0;

	while (str[i] != '\0')
	{
	putchar(str[i]);
	i += 0;
	}
	putchar('\n');
}
