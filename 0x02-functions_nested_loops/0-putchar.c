#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char str[] = "_putchari\n";

	int ch;

	for (ch = 0; ch < 8; ch++)

		_putchar(str[ch]);

	return (0);
}
