#include "main.h"

/**
 * _abs - funvtion that calculates the value absolute of a numbeer
 * 
 * @n: inter from input
 *
 * Return: always abs(n)
 *
 */

int _abs(int n)
{
	if (n < 0)
	{
		n = (-1) * n;
	return (n);
	}
	else
		return (n);
}
