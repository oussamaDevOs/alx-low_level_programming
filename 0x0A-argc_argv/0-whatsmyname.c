#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 * @argc: argument counteer
 * @argv: argument vector
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
